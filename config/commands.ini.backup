; =============================
; Commands Layer Configuration
; =============================
; =============================
; Confirmation configuration — How to configure
; =============================
; 1) Global (optional): in configuration.ini set [Behavior] show_confirmation_global=true to confirm everything.
; 2) Category (optional): in this file under [CategorySettings] set <Friendly>_show_confirmation=true to confirm all keys in that category; set false to control per key.
; 3) Per-command lists (recommended): in [Confirmations.<Friendly>]
;    - confirm_keys= K1,K2 ... list the keys that must confirm (case-sensitive: R != r)
;    - no_confirm_keys= K1 K2 ... optional blacklist of keys that must NOT confirm
;      Use confirm_keys when default is not to confirm; use no_confirm_keys when you confirm by default.
; 4) Layer default (optional): in [Settings] show_confirmation=true to confirm by default across Commands layer.
; Examples:
; - HybridManagement: confirm only Reload Script: confirm_keys=R
; - PowerOptions: confirm Shutdown and Restart: confirm_keys=S,r,o
; - Confirm all by default but skip Sleep/Hibernate: set [Settings].show_confirmation=true and no_confirm_keys=s h
; Tips: prefer the shortest list, and reload the script after changes (<leader> → c → h → R).
;


[Categories]
order=s h g m n f w o a t x 
s=System Commands
h=Hybrid Management
g=Git Commands
m=Monitoring Commands
n=Network Commands
f=Folder Access
o=Power Options
a=ADB Tools

[s_category]
title=System Commands
order=s t v e d c h r E
s_show_confirmation=false           ;cofirm category, if true all keys confirm, if false, check per-key lists below in confrm_keys/no_confirm_keys
confirm_keys=                       ;confirm only these keys (case-sensitive) 
no_confirm_keys=                    ;confirm all except these keys (case-sensitive)
s=System Info
t=Task Manager
v=Services
e=Event Viewer
d=Device Manager
c=Disk Cleanup
h=Toggle Hidden Files
r=Registry Editor
E=Environment Variables

[h_category]
title=Hybrid Management
order=R p l c v s e k
s_show_confirmation=false           ;cofirm category, if true all keys confirm, if false, check per-key lists below in confrm_keys/no_confirm_keys
confirm_keys=R e                    ;confirm only these keys (case-sensitive) 
no_confirm_keys=                    ;confirm all except these keys (case-sensitive)
R=Reload HybridCapsLock
p=Pause Hybrid 
l=View log file
c=Open config folder
v=Show version info
s=Show System Status
e=Exit Script
k=reload kanata

[g_category]
title=Git Commands
order=s l b d a p
s_show_confirmation=false           ;cofirm category, if true all keys confirm, if false, check per-key lists below in confrm_keys/no_confirm_keys
confirm_keys=a, p                   ;confirm only these keys (case-sensitive) 
no_confirm_keys=                    ;confirm all except these keys (case-sensitive)
s=Git Status
l=Git Log
b=Git Branches
d=Git Diff
a=Git Add All
p=Git Pull

[m_category]
title=Monitoring Commands
order=p s d m c
s_show_confirmation=false           ;cofirm category, if true all keys confirm, if false, check per-key lists below in confrm_keys/no_confirm_keys
confirm_keys=                       ;confirm only these keys (case-sensitive) 
no_confirm_keys=                    ;confirm all except these keys (case-sensitive)
p=Process List
s=Service List
d=Disk Space
m=Memory Usage
c=CPU Usage

[n_category]
title=Network Commands
order=i p n
s_show_confirmation=false           ;cofirm category, if true all keys confirm, if false, check per-key lists below in confrm_keys/no_confirm_keys
confirm_keys=                       ;confirm only these keys (case-sensitive) 
no_confirm_keys=                    ;confirm all except these keys (case-sensitive)
i=IP Config
p=Ping Test
n=Network Info

[f_category]
title=Folder Access
order=t a p u d s
s_show_confirmation=false           ;cofirm category, if true all keys confirm, if false, check per-key lists below in confrm_keys/no_confirm_keys
confirm_keys=                       ;confirm only these keys (case-sensitive) 
no_confirm_keys=                    ;confirm all except these keys (case-sensitive)
t=Temp Folder
a=AppData
p=Program Files
u=User Profile
d=Desktop
s=System32

[v_category]
title=VaultFlow
order=v
s_show_confirmation=false           ;cofirm category, if true all keys confirm, if false, check per-key lists below in confrm_keys/no_confirm_keys
confirm_keys=                       ;confirm only these keys (case-sensitive) 
no_confirm_keys=                    ;confirm all except these keys (case-sensitive)
v=Launch VaultFlow

[o_category]
title=Power Options
order=s h r u l o
s_show_confirmation=false           ;cofirm category, if true all keys confirm, if false, check per-key lists below in confrm_keys/no_confirm_keys
confirm_keys=S r o                     ;confirm only these keys (case-sensitive) 
no_confirm_keys=                    ;confirm all except these keys (case-sensitive)
s=Sleep
h=Hibernate
r=Restart
S=Shutdown
l=Lock
o=Sign Out

[a_category]
title=ADB Tools
order=d x s l r
s_show_confirmation=false           ;cofirm category, if true all keys confirm, if false, check per-key lists below in confrm_keys/no_confirm_keys
confirm_keys=                       ;confirm only these keys (case-sensitive) 
no_confirm_keys=                    ;confirm all except these keys (case-sensitive)
d=ADB Devices
x=ADB Disconnect
s=ADB Shell
l=ADB Logcat
r=ADB Reboot

[CustomCommands]
VaultFlow=ps:vaultflow
Sleep=cmd:rundll32.exe powrprof.dll,SetSuspendState 0,1,0
Shutdown=cmd:shutdown /s /t 0
Hibernate=cmd:rundll32.exe powrprof.dll,SetSuspendState Hibernate
Restart=cmd:shutdown /r /t 0
ADBDevices=cmd:adb devices
ADBDisconnect=cmd:adb disconnect
ADBShell=cmd:adb shell
ADBLogcat=cmd:adb logcat
ADBReboot=cmd:adb reboot

; --- Example Custom Commands for Tools category ---
GitStatus=cmd:git status
OpenDocs=url:https://example.com/docs

[CommandFlag.GitStatus]
terminal=conhost
keep_open=true
working_dir=%USERPROFILE%\projects\acme

[CommandFlag.OpenDocs]
; no flags needed; opens default browser

[Settings]
timeout_seconds=30
show_confirmation=false                    ; Ask confirmation on Commands (Power Options). If [Behavior].show_confirmation_global=true, global wins

